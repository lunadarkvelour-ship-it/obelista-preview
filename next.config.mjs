/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Корень отдаёт публичную страницу (app/page.tsx), а не панель.
  //
  // Раньше здесь стоял редирект `/` → `/launch`. Локально это было удобно, но
  // наружу выходило ловушкой: панель ходит за данными на 127.0.0.1, и любой,
  // кто открыл сайт со своей машины, видел пустой каркас. Ревьюер Меты
  // открывает Site URL первым делом — и закрывает заявку с «could not test
  // the app». Панель открывается с главной по ссылке «Open the panel».
};
export default nextConfig;
