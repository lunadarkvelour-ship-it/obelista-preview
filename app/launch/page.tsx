import { redirect } from "next/navigation";

/** Конструктор одной связки на один соц уехал в путь «Кабинеты → Аплоад →
 *  Превью». Адрес оставлен живым: он лежит в закладках и в старых заметках,
 *  и пустая страница по нему выглядела бы как поломка панели. */
export default function Page() {
  redirect("/accounts");
}
