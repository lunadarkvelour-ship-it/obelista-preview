"use client";

/* Один пункт меню «Кабинеты» — и ОДИН лист, без развилок (иссус #121).
 *
 * ЗДЕСЬ БЫЛА РАЗВИЛКА ПО ИСТОЧНИКУ: есть снапшот с мака — рисуем `AccountsView`,
 * нет — `CloudAccountsView` из базы. Выглядело это разумно ровно один день. Дальше
 * получилось так: у владельца снапшот есть, и он видел лист с поиском, выбором и
 * группами; у свежей учётки снапшота нет, и она видела совсем другой экран —
 * сводку с разрезом по Business Manager. Один продукт, два непохожих лица, и
 * править их приходилось дважды. Владелец сказал про них «они между собой спорят»,
 * и спорили именно КОПИИ, а не замыслы.
 *
 * Ещё дороже была вторая часть: развилка стояла на НАЛИЧИИ снапшота, поэтому у
 * владельца ручка `/accounts` не звалась вовсе, а в облаке не звался снапшот. Две
 * половины данных об одном кабинете никогда не встречались на одном экране.
 *
 * Теперь источники сводятся до разметки (`lib/cloud-accounts.unifiedAccounts`), и
 * страница не выбирает экран по тому, что успело приехать. Пустой снапшот у нового
 * арендатора — норма, а не повод показать ему другой продукт.
 */

import { AccountsView } from "@/components/views/AccountsView";

export default function Page() {
  return <AccountsView />;
}
